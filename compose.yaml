version: "3.9"

services:
  backend:
    build: ./backend
    container_name: reread-backend
    ports:
      - "3000:3000"
    environment:
      - PORT=3000

  frontend:
    build: ./frontend
    container_name: reread-frontend
    ports:
      - "5173:5173"
    depends_on:
      - backend
